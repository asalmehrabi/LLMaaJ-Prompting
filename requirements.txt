# ─────────────────────────────────────────────────────────────────────
# Thesis Pipeline — Requirements
# Install: pip install -r requirements.txt
# On Snellius: use scripts/setup_env.sh instead (handles modules + venv)
# ─────────────────────────────────────────────────────────────────────

# Core ML / inference
vllm==0.6.6
transformers>=4.45.0
torch>=2.4.0

# Data handling
datasets==3.5.0
pandas>=2.0
pyarrow>=14.0
numpy>=1.24

# Evaluation metrics
scipy>=1.11
scikit-learn>=1.3

# API judges (install separately if needed)
# Uncomment the one(s) you need:
openai>=1.0
# google-cloud-aiplatform>=1.38

# Analysis / visualization (for notebooks)
# matplotlib>=3.7
# seaborn>=0.13
# plotly>=5.18
